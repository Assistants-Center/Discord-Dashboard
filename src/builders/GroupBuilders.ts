import {
    UserFormGroupBuilder,
    GuildFormGroupBuilder,
} from '@discord-dashboard/typings/dist/FormOptions/FormGroupBuilders';

const GroupBuilders = {
    Guild: GuildFormGroupBuilder,
    User: UserFormGroupBuilder,
};

export default GroupBuilders;
